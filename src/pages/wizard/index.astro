---
import WizardPrototypeLayout from '../../layouts/WizardPrototypeLayout.astro';
import {
  DEMO_STAGE_LABELS,
  DEMO_REPORT_CHAPTERS,
  DEMO_SOURCE_TYPE_LABELS,
  DEMO_APPROVERS,
  TOTAL_PREFLIGHT_FINDINGS,
} from '../../data/wizard-prototype';

const STAGE_DESCRIPTIONS: Record<string, string> = {
  materials:
    '「数字の食い違い」「出典が出せない」をここで引き受けます。資料から根拠を書き出して確認し、主張の裏づけとなる根拠リストを作ります。',
  meaning:
    '資料ごとの言い方のブレをここで揃えます。確認した言い方が6枚の判断カードになり、以降の原稿と点検の基準になります。',
  preflight:
    '根拠のない優越表現や、対象外用途との混同を、公開前に検出します。直すか、理由付きで例外受容するまで、クライアント確認には進みません。',
  approval:
    '「誰がいつ、どの版を承認したか」をここで記録します。根拠・出典・点検結果がそろった状態でクライアントに提示します。',
  results:
    '「効果はどうでしたか」に、公開前後の記録で答えます。因果効果は保証せず、実測の記録と、次にすべきこと最大3件を残します。',
};

const stageOverview = DEMO_STAGE_LABELS.map((s) => ({ ...s, description: STAGE_DESCRIPTIONS[s.key] }));
---
<WizardPrototypeLayout title="はじめに・使い方" description="Meandle Wizard Partner の概要・使い方・よくある質問">
  <header class="wp-page-header" slot="header">
    <div class="wp-page-header__top">
      <h1>Meandle Wizard Partner</h1>
    </div>
    <p class="wp-page-header__sub">
      見込み客は、問い合わせの前にAIにも尋ねる時代です。人が読んで分かるのと同じ正確さで、AIにも間違いなく商品を把握させる——そのために整えるべきは、公開物の根拠と言い方の一貫性です。Meandle Wizard Partner は、プレスリリース・LP・営業資料を根拠つきで整え、承認の記録からクライアントへの報告までを一つの流れで残す、代理店パートナー向けの操作ツールです。
    </p>
  </header>

  <section class="wp-card" aria-labelledby="problem-heading">
    <h2 id="problem-heading">こんな場面のためのツールです</h2>
    <p style="font-size:13px;color:var(--wp-ink-muted);margin:0 0 12px;">
      B2Bの制作現場で繰り返される、次のような場面を想定しています。
    </p>
    <ul class="wp-list" style="font-size:14px;line-height:1.8;">
      <li>LPでは月額25万円、営業資料では30万円——資料ごとに数字が食い違ったまま、公開予定日が近づいている</li>
      <li>原稿の「業界No.1」について、クライアントに「出典は？」と聞かれて出せる記録がない</li>
      <li>承認のやり取りがメールとチャットに散らばり、どの版を誰がいつ承認したのか、あとから辿れない</li>
      <li>公開のあと「効果はどうでしたか」と聞かれても、公開前と比べられる記録がない</li>
      <li>AIに商品のことを尋ねると、対象外の用途や古い情報と混ざった説明が返ってくる</li>
    </ul>
    <p style="font-size:13px;color:var(--wp-ink-muted);margin:16px 0 0;">
      こうした場面の多くは、根拠の確認・言い方の統一・承認の記録が、人の記憶とメール箱に散らばったまま進むことから生まれます。そして、公開物に残った食い違いや根拠のない表現は、人だけでなくAIの説明にもそのまま現れます。Meandle Wizard Partner は、その全部を案件ごとの一つの流れに集め、進めるたびに記録として残します。
    </p>
  </section>

  <section class="wp-card wp-entry-reveal" aria-labelledby="entry-preview-heading">
    <h2 id="entry-preview-heading" style="font-size:16px;">案件が最後に手渡すもの</h2>
    <p style="font-size:13px;color:var(--wp-ink-muted);margin:0 0 12px;">
      確認済みの根拠リスト／クライアントの承認記録／公開前後の記録を同じ条件で比べるレポート。進めるほど、この3つが確定していきます。
    </p>
    <div class="wp-form-grid">
      <div>
        <p style="font-size:12px;color:var(--wp-ink-muted);letter-spacing:0.04em;margin:0 0 8px;text-transform:uppercase;">
          レポートの構成（{DEMO_REPORT_CHAPTERS.length}章）
        </p>
        <ol style="margin:0;padding-left:20px;font-size:12px;color:var(--wp-ink-muted);line-height:1.9;">
          {DEMO_REPORT_CHAPTERS.map((c) => (<li>{c}</li>))}
        </ol>
      </div>
      <div>
        <p style="font-size:12px;color:var(--wp-ink-muted);letter-spacing:0.04em;margin:0 0 8px;text-transform:uppercase;">
          見本（架空データ・RelayOneデモ案件）
        </p>
        <p style="font-size:13px;color:var(--wp-ink-muted);background:var(--wp-page);border:1px solid var(--wp-border);border-radius:8px;padding:12px 16px;">
          根拠 0件 ・ 出典 0/{DEMO_SOURCE_TYPE_LABELS.length}種 ・ 承認 0/{DEMO_APPROVERS.length}名 ・ 点検 0/{TOTAL_PREFLIGHT_FINDINGS}項目
        </p>
      </div>
    </div>
  </section>

  <div class="wp-next-action">
    <p class="wp-next-action__label">操作デモ・架空データ</p>
    <h2>まずは動かしてみる</h2>
    <p>
      RelayOneという架空のB2B案件で、上のレポートができあがるまでの流れを実際に操作できます。ブラウザのlocalStorageにのみ保存され、サーバーへは送信されません。いつでも「デモを初期状態に戻す」で最初からやり直せます。
    </p>
    <a class="wp-btn wp-btn--on-dark" href="/wizard/demo/">操作デモを試す →</a>
  </div>

  <section class="wp-card" aria-labelledby="overview-heading">
    <h2 id="overview-heading">どう解決するのか — 5つの段階</h2>
    <p style="font-size:13px;color:var(--wp-ink-muted);margin:0 0 16px;">
      案件は次の5段階で進みます。それぞれが冒頭の場面のどれかを引き受け、終わるたびに成果物が一つ残ります。承認や公開そのものを自動化するものではなく、判断のための記録と点検を助けるツールです。
    </p>
    <div class="wp-form-grid">
      {stageOverview.map((s) => (
        <div class="wp-card wp-card--tight">
          <h3 style="font-size:14px;">{s.label}</h3>
          <p style="font-size:13px;color:var(--wp-ink-muted);margin:0 0 8px;">{s.description}</p>
          <p style="font-size:12px;color:var(--wp-ink-muted);">
            この段階で作るもの: <strong style="color:var(--wp-ink-strong);">{s.makes}</strong>
          </p>
        </div>
      ))}
    </div>
  </section>

  <section class="wp-card" aria-labelledby="tutorial-heading">
    <h2 id="tutorial-heading">はじめての操作（チュートリアル）</h2>
    <p style="font-size:13px;color:var(--wp-ink-muted);margin:0 0 16px;">
      実際に触ると、6つのステップが下の説明のとおりに進み、レポートへ積み上がっていくのを確認できます。架空データ（RelayOneデモ案件）で動作します。
    </p>

    <div class="wp-card wp-card--tight" style="margin-bottom:12px;">
      <div class="wp-tutorial-step">
        <span class="wp-tutorial-step__mark" aria-hidden="true">1</span>
        <div class="wp-tutorial-step__body">
          <h3>案件を作成する</h3>
          <p>基本情報を入力すると、この案件のレポートの構成がすぐに用意されます。</p>
          <a class="wp-btn wp-btn--ghost" href="/wizard/campaign/new/">案件作成画面を見る →</a>
        </div>
      </div>
    </div>

    <div class="wp-card wp-card--tight" style="margin-bottom:12px;">
      <div class="wp-tutorial-step">
        <span class="wp-tutorial-step__mark" aria-hidden="true">2</span>
        <div class="wp-tutorial-step__body">
          <h3>根拠を揃える</h3>
          <p>資料間の食い違いが解消され、主張の裏づけとなる根拠リストができます。</p>
          <a class="wp-btn wp-btn--ghost" href="/wizard/campaign/demo/materials/">根拠を揃える画面を見る →</a>
        </div>
      </div>
    </div>

    <div class="wp-card wp-card--tight" style="margin-bottom:12px;">
      <div class="wp-tutorial-step">
        <span class="wp-tutorial-step__mark" aria-hidden="true">3</span>
        <div class="wp-tutorial-step__body">
          <h3>伝え方を決める</h3>
          <p>確認した言い方が、6枚の判断カードとして、この案件の基準になります。</p>
          <a class="wp-btn wp-btn--ghost" href="/wizard/campaign/demo/meaning/">伝え方を決める画面を見る →</a>
        </div>
      </div>
    </div>

    <div class="wp-card wp-card--tight" style="margin-bottom:12px;">
      <div class="wp-tutorial-step">
        <span class="wp-tutorial-step__mark" aria-hidden="true">4</span>
        <div class="wp-tutorial-step__body">
          <h3>公開前に点検する</h3>
          <p>根拠のない優越表現や、対象外用途との混同が、公開前に見つかります。</p>
          <a class="wp-btn wp-btn--ghost" href="/wizard/campaign/demo/preflight/">公開前チェック画面を見る →</a>
        </div>
      </div>
    </div>

    <div class="wp-card wp-card--tight" style="margin-bottom:12px;">
      <div class="wp-tutorial-step">
        <span class="wp-tutorial-step__mark" aria-hidden="true">5</span>
        <div class="wp-tutorial-step__body">
          <h3>承認を得て公開する</h3>
          <p>根拠・出典・点検結果がそろった状態で、クライアントに提示されます。承認は氏名と日時つきの記録として残ります。</p>
          <div style="display:flex;gap:8px;flex-wrap:wrap;">
            <a class="wp-btn wp-btn--ghost" href="/wizard/campaign/demo/approval/preview/">代理店側のプレビューを見る →</a>
            <a class="wp-btn wp-btn--ghost" href="/wizard/approval/demo/">クライアント側の確認画面を見る →</a>
          </div>
        </div>
      </div>
    </div>

    <div class="wp-card wp-card--tight" style="margin-bottom:16px;">
      <div class="wp-tutorial-step">
        <span class="wp-tutorial-step__mark" aria-hidden="true">6</span>
        <div class="wp-tutorial-step__body">
          <h3>レポートと次の一手</h3>
          <p>公開前後の変化が記録され、クライアントに渡せるレポートになります。次にすべきことは、最大3件に絞られます。</p>
          <div style="display:flex;gap:8px;flex-wrap:wrap;">
            <a class="wp-btn wp-btn--ghost" href="/wizard/campaign/demo/results/">結果画面を見る →</a>
            <a class="wp-btn wp-btn--ghost" href="/wizard/campaign/demo/report/">レポート画面を見る →</a>
          </div>
        </div>
      </div>
    </div>

    <a class="wp-btn wp-btn--primary" href="/wizard/demo/">続きはダッシュボード（今日の作業）から →</a>
  </section>

  <section class="wp-card" aria-labelledby="faq-heading">
    <h2 id="faq-heading">よくある質問</h2>

    <details class="wp-faq-item">
      <summary>誰のためのツールですか？</summary>
      <div class="wp-faq-item__body">
        <p>Meandleの代理店パートナー（PR会社・広告代理店・制作会社）向けです。クライアント案件のプレスリリース・LP・営業資料を、根拠を残しながら仕上げる場面を想定しています。</p>
      </div>
    </details>

    <details class="wp-faq-item">
      <summary>これは何をするツールですか？</summary>
      <div class="wp-faq-item__body">
        <p>代理店パートナー向けの制作支援ツールです。根拠を揃える→伝え方を決める→公開前に点検する→承認を得て公開→レポートと次の一手、という一連の作業を、案件ごとに記録しながら進められます。</p>
      </div>
    </details>

    <details class="wp-faq-item">
      <summary>AIに正しく把握されることと、このツールの関係は？</summary>
      <div class="wp-faq-item__body">
        <p>見込み客が問い合わせの前にAIへ尋ねる場面が増えています。AIの説明は、公開されたリリース・LP・資料の記述をもとに組み立てられるため、数字の食い違いや根拠のない表現は、誤った説明の材料になり得ます。このツールで根拠と言い方を揃えて公開することは、AIが人と同じように間違いなく商品を把握するための土台づくりです。</p>
      </div>
    </details>

    <details class="wp-faq-item">
      <summary>AIが自動で文章を作ってくれますか？</summary>
      <div class="wp-faq-item__body">
        <p>AIは候補を出しますが、「確認済み」にするのは人だけです。該当なしを選ぶ場合も、理由の入力が必須です。</p>
      </div>
    </details>

    <details class="wp-faq-item">
      <summary>入力したデータはどこに保存されますか？</summary>
      <div class="wp-faq-item__body">
        <p>このブラウザのlocalStorageにのみ保存され、サーバーへは送信されません。この操作デモは架空データ専用です。実在企業の資料や機密情報は入力しないでください。</p>
      </div>
    </details>

    <details class="wp-faq-item">
      <summary>「業界No.1」のような表現は使えませんか？</summary>
      <div class="wp-faq-item__body">
        <p>根拠（第三者調査・出典など）があれば使えます。根拠のない優越表現は公開前チェックで検出され、修正するか、代理店レビュー担当が理由付きで例外として受け入れるまで、先には進めません。</p>
      </div>
    </details>

    <details class="wp-faq-item">
      <summary>承認や点検の記録はどこに残りますか？</summary>
      <div class="wp-faq-item__body">
        <p>承認者・承認日時・点検結果は、クライアントに渡せるレポートにまとめて記録されます。</p>
      </div>
    </details>

    <details class="wp-faq-item">
      <summary>効果は保証されますか？</summary>
      <div class="wp-faq-item__body">
        <p>因果効果は保証しません。問い合わせ数などの変化には複数の要因が影響するため、レポートでも実数・母数・取得日・出典・方法・限界をあわせて記載します。</p>
      </div>
    </details>

    <details class="wp-faq-item">
      <summary>今はどの案件で試せますか？</summary>
      <div class="wp-faq-item__body">
        <p>現在はRelayOneデモ案件（架空データ）で一連の流れを確認できます。「デモを初期状態に戻す」で、いつでも最初からやり直せます。</p>
      </div>
    </details>

    <details class="wp-faq-item">
      <summary>他のMeandleサービスとの違いは？</summary>
      <div class="wp-faq-item__body">
        <p>do.meandle・Meandle PR・Meandle get leadなどは、制作物そのものをMeandleが手がけるサービスです。Meandle Wizard Partnerは、その制作物を代理店パートナーが根拠を残しながら仕上げるための操作ツールです。</p>
      </div>
    </details>
  </section>
</WizardPrototypeLayout>
